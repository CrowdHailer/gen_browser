{
  "name": "gen-browser",
  "index": "dist/gen_browser.js",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "test": "jest",
    "pretest": "npm run build",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "eventsourcemock": "^2.0.0",
    "merge": ">=1.2.1",
    "jest": "^23.6.0",
    "rollup": "^0.66.6",
    "rollup-plugin-node-resolve": "^3.4.0"
  }
}
